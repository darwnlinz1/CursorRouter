# MASTER PROMPT FOR CURSOR: BUILD "CYBER-OS 2077" WEB DESKTOP IN ONE SHOT

You are an elite Principal Frontend Systems Architect, UI/UX Designer, and HTML5 Game/Audio Engineer.
Your mission is to generate a PRODUCTION-READY, FULLY FUNCTIONAL Web Desktop Operating System named:

# "CYBER-OS 2077"

The entire application must be contained in ONE SINGLE file: `index.html` (embedded `<style>` and `<script>`).
It must run directly by opening `index.html` in Google Chrome / Edge without any build tools, npm, or external servers.

---

## 1. ABSOLUTE CONSTRAINTS & PRIME DIRECTIVES
1. NO PLACEHOLDERS: Every single button, menu, window, and game must have 100% working logic. Writing "// TODO", "// implement later", or placeholder comments is STRICTLY PROHIBITED.
2. ZERO EXTERNAL LIBS: Use only Vanilla HTML5, CSS3, JavaScript (ES6+), HTML5 Canvas, Web Audio API, and localStorage.
3. VISUAL QUALITY: Futuristic Cyberpunk Dark HUD theme (void black `#0a0c14`, neon cyan `#00f3ff`, hot magenta `#ff0055`, amber `#ffb800`, scanline effects, glowing borders, frosted glass blur).

---

## 2. CORE OPERATING SYSTEM FEATURES (WINDOW MANAGER)
- Desktop Icons: Click to launch apps with open animation.
- Taskbar & Start Menu: Bottom taskbar with system clock, active window pills, start button, and audio volume toggle.
- Full Window Management:
  * Draggable window titlebars.
  * Minimize, Maximize / Restore, and Close buttons.
  * Z-index stacking (clicking any window brings it to the top).
  * Smooth resize handles.

---

## 3. 5 FULLY WORKING BUILT-IN APPLICATIONS REQUIRED

### APP 1: CYBER-SHOOTER (HTML5 Canvas Arcade Game)
- Full arcade space shooter: player spaceship, laser firing, enemy alien waves, asteroid dodging, particle explosions, score counter, power-up drops (Shield, Multishot), and Game Over / Restart state.

### APP 2: NEON SYNTH (Web Audio API Synthesizer & Visualizer)
- Working 8-key interactive piano/synth keyboard.
- Oscillators: Sine, Square, Sawtooth, Triangle toggles.
- Real-time Web Audio Analyser showing an animated glowing frequency visualizer on a canvas.
- Reverb / Delay toggle and volume sliders that produce real sound through speakers.

### APP 3: PIXEL MATRIX TERMINAL (Command Line Interface)
- Functional terminal emulator with blinking cursor and command input.
- Real working commands:
  * `help`: lists all commands.
  * `matrix`: toggles classic falling green digital rain canvas animation.
  * `calc <expr>`: evaluates mathematical expressions safely.
  * `echo <text>`: prints text.
  * `sysinfo`: prints browser hardware info, screen resolution, user agent.
  * `clear`: clears terminal history.

### APP 4: SPRITE / PIXEL ART EDITOR
- 16x16 grid canvas pixel editor.
- Color palette picker (8 cyberpunk colors + eraser).
- Pencil tool, Fill bucket tool, and Clear grid.
- Export / Download drawing as PNG or Save to localStorage.

### APP 5: SYSTEM TASK MANAGER & HARDWARE MONITOR
- Live real-time CPU & Memory graph simulator drawn on Canvas with scrolling waves.
- Process list showing running OS windows/apps with RAM consumption and "Kill Process" button that actually closes the window.
- System uptime counter ticking in seconds.

---

## 4. OUTPUT INSTRUCTIONS
Produce the entire complete, runnable `index.html` file right now from `<!DOCTYPE html>` to `</html>`. 
Do not truncate. Output the entire working code in one continuous response.
